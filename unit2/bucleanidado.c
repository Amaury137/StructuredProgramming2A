"pendiente"
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#define TAN 10

int lista[TAN]={23,23,435,65,86,34,897,345,87,34}
int idx = 0;

struct nodePr{
    int size;
    struct node{
        char* str;
    };
}NODE;
struct Noder myStruct = {1,{"hola"}};
NODE myStruct = {1,{"hola"}};

typedef int INTEGER; 

int main(int argc, char** argv){
    int index2 = 0;
    for (int index=0; index <= TAN; INDEX++, index2++){
        printf("index: %d, value %d, \n\a", index, lista[index]);
        for(size_t i = 0; i<3; i++){
            printf("Hola mundo\n");
        }

    }
    return 0;
}
